#[derive(Debug, Clone)]
pub struct Pedido {
    /// Cantidad de cafe molido
    pub cafe_molido: i32,
    /// Cantidad de agua caliente
    pub agua_caliente: i32,
    /// Cantidad de cacao
    pub cacao: i32,
    /// Cantidad de espuma
    pub espuma: i32,
}
